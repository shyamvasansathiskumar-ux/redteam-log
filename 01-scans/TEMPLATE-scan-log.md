# Scan log — [target model] — [YYYY-MM-DD]

> Copy this file, rename to `YYYY-MM-DD-<model>-<probe>.md`, fill it in as you go.
> Ten minutes of writing here is what turns a terminal command into a portfolio artifact.

## Scope

| Field | Value |
|---|---|
| Target | e.g. `llama3.2:3b` via Ollama, local |
| Tool + version | e.g. garak `x.y.z` |
| Probes run | e.g. `promptinject` |
| Generations | e.g. 1 |
| Date | YYYY-MM-DD |
| Time spent | e.g. 45 min |

**Why this target and these probes:** *(one or two sentences — "picked promptinject because it maps to LLM01, the class I read about Friday." A scan without a stated reason reads as random button-pushing.)*

## Command

```bash

```

## Results summary

| Probe | Attempts | Hits | Hit rate |
|---|---|---|---|
| | | | |

## Notable findings

For each interesting hit — the prompt, what came back, and why it matters. Redact anything genuinely harmful in the output; describe the category instead of reproducing it in full.

### Finding 1 — *[short name]*

- **Attack prompt (abridged):**
- **Model response (abridged):**
- **Why it matters:**
- **OWASP LLM class:** e.g. LLM01 Prompt Injection
- **MITRE ATLAS technique:** e.g. AML.Txxxx — *(look it up; leave blank rather than guessing)*
- **Severity, and your reasoning:** low / medium / high —

## What surprised me

*(The single most valuable section for your own learning, and the one that reads as real experience to anyone reviewing this later. What did you expect that didn't happen?)*

## What I'd do differently next run

-

## Governance translation

*One paragraph, written as if for someone who will never read the terminal output.* What would a risk owner need to know? What control would reduce this? This paragraph is the bridge — it's the exact skill the market says it can't find, and practising it weekly is why it will be there when you need it.

---

**Follow-ups**

- [ ]
